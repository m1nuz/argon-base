#include <stdlib.h>
#include <string.h>

#ifdef USE_MEMTRACK
#include <memtrack.h>
#endif

#include "base/block_alloc.h"

/*struct BlockPool {
};

extern BLOCK_POOL*
new_block_pool(size_t num, size_t block_size) {
}

extern void
free_block_pool(BLOCK_POOL *pool) {
}

static inline void*
AddrFromIndex(BLOCK_POOL *pool, size_t i) {
}

static inline size_t
IndexFromAddr(BLOCK_POOL *pool, const void *p) {
}

extern void*
block_alloc(BLOCK_POOL *pool) {
}

extern void
block_free(BLOCK_POOL *pool, void* obj) {
}*/
